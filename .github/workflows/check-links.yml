name: check-links

on:
  push:
    branches:
      - "iss-152"
#  schedule:
#    - cron: '15 17 * * 1'

jobs:
  check-links:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:

      - name: Checkout the lesson
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Set up R
        uses: r-lib/actions/setup-r@v1
        with:
          r-version: 'release'

      - name: Install needed packages
        run: |
          install.packages(c('rvest', 'purr', 'dplyr', 'httr'))
        shell: Rscript {0}

      - name: Check URL links
        run: |
          source("bin/detect-broken-links.R")
          detect_broken_links("https://umcarpentries.org/intro-curriculum-r")
        shell: Rscript {0}
